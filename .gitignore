# Editors / IDEs
.idea/
.vscode/
*.swp
.DS_Store

# Claude Code local (machine-specific) settings
.claude/settings.local.json

# Python
__pycache__/
*.pyc
.venv/
venv/
.ruff_cache/

# Local logs / scratch
*.log
*.err

# Real cluster hostfile (contains usernames/IPs) — commit only the .example
cluster/hostfile.json

# Personal reference screenshots / scratch — not for the repo
sample/
